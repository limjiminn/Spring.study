<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="BoardMapper">
  	<insert id="create" parameterType="com.springstudy.shop.domain.BoardDTO">
  		INSERT INTO tbl_board(bno, title, content, writer)
  		VALUES(board_seq.NEXTVAL, #{title}, #{content}, #{writer})
  		<selectKey keyProperty="bno" resultType="Integer" order="AFTER">
  			SELECT board_seq.currval FROM dual
  			<!-- 현제 발급받은 값 -->
  		</selectKey>
  	</insert>
  	
  	<select id="read" resultType="com.springstudy.shop.domain.BoardDTO">
  		SELECT *
  		FROM tbl_board
  		WHERE bno = #{bno}
  	</select>
  	
  	<update id="update" >
  		UPDATE tbl_board
  		SET title = #{title}, content = #{content}
  		WHERE bno = #{bno}
  	</update>
  	
  	<delete id="delete">
  		DELETE FROM tbl_board
  		WHERE bno = #{bno}
  	</delete>
  	
  	<select id="getListWithPaging" resultType="com.springstudy.shop.domain.BoardDTO">
  		<![CDATA[
	  		SELECT bno,
	  			   title,
	  			   content,
	  			   writer,
	  			   regdate,
	  			   viewcnt
	  		FROM (      		
	  				SELECT /*+ INDEX_DESC(tbl_board IDX_TBL_BOARD_PK)*/
	  						rownum rn,
	  						bno,
	  						title,
	  						content,
	  						writer,
	  						regdate,
	  						viewcnt
	  				FROM 	tbl_board
	  				WHERE	rownum <= #{pageNum} * #{amount})
	  		WHERE rn > (#{pageNum} - 1) * #{amount}
  		]]>
  	</select>
  	<select id="getTotalCnt" resultType="int">
  		SELECT COUNT(*)
  		FROM	tbl_board
  		WHERE bno > 0
  	</select>
  </mapper>